spring:
  application:
    name: Claim-management
  data:
    mongodb:
      uri: mongodb://localhost:27017/user-service-db
  docker:
    compose:
      enabled: false
  security:
    user:
      name: admin
      password: admin

server:
  port: 9091
